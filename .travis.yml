
.travis.yml:

install:
  - npm install bcrypt@0.8.5 /home/travis/build/denvereezy/all-about-music/node_modules/encryptonator/node_modules/bcrypt
  - npm install -g npm@2

language: node_js
node_js:
  - "6.0.0"

before_script:
  - npm install
  - mysql -u root -e 'create database travis_db;'
  - mysql -u root travis_db < tables.sql

script: mocha
